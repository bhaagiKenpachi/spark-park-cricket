global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    environment: 'prod'
    cluster: 'cricket-prod-cluster'

scrape_configs:
  # Prometheus itself
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # Production backend metrics
  - job_name: 'cricket-backend-prod'
    static_configs:
      - targets: ['cricket.dojima.foundation:443']
    metrics_path: '/metrics'
    scheme: 'https'
    scrape_interval: 15s
    scrape_timeout: 10s
    tls_config:
      insecure_skip_verify: true

  # Production frontend metrics (if available)
  - job_name: 'cricket-frontend-prod'
    static_configs:
      - targets: ['cricket.dojima.foundation:443']
    metrics_path: '/api/metrics'
    scheme: 'https'
    scrape_interval: 30s
    scrape_timeout: 10s
    tls_config:
      insecure_skip_verify: true

# Remote write configuration to send metrics to central Prometheus (optional)
# remote_write:
#   - url: "http://central-prometheus:9090/api/v1/write"
#     queue_config:
#       max_samples_per_send: 1000
#       batch_send_deadline: 5s

# Alerting rules
rule_files:
  - "rules/*.yml"

# Alertmanager configuration
alerting:
  alertmanagers:
    - static_configs:
        - targets:
          - alertmanager:9093
