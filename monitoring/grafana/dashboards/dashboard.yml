apiVersion: 1

providers:
  - name: 'cricket-dev-dashboards'
    orgId: 1
    folder: 'Dev Environment'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
